<% a = ['avatar1.png', 'avatar2.png', 'avatar3.png', 'avatar4.png', 'avatar5.png'] %>

<div class="banner-step2">
    <div class="avatar-step2">
    <h1><%= @project.name %></h1>
      <% @guests.each do |guest| %>
        <% if guest.user.picture.present? %>
          <%= cl_image_tag guest.user.picture, :aspect_ratio=>"1:1", :background=>"#262c35", :gravity=>"auto", :radius=>"max", :crop=>"fill" , class:"avatar" %>
          <% else %>
          <%= image_tag a.sample %>
        <% end %>
      <% end %>
    </div>
</div>
<div class="step1-text" data-aos="fade-right" data-aos-duration="1000">
  <h3>Step 1</h3>
  <h1>Define the mood</h1>
  <p>Before anything else we need to ask you a few questions to stage your week-end. You should always keep in mind matching the groom or bride's tastes to create a truly amazing party that will be remembered!
  </p>
</div>
<div class="ideas-done">
  <p>4 questions · <i class="fas fa-stopwatch"></i> 3 minutes</p>
    <%= link_to 'Start', scenery_project_moods_path(@project), class: "button_primary hvr-pop" %>
</div>
